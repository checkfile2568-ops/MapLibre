# บันทึกการเปลี่ยนแปลง

## Version 5.0.1 — 27 สิงหาคม 2569

- **กดชื่อผู้รับผิดชอบแล้วแผนที่ค่อย ๆ ซูมเข้าไปที่พื้นที่ของคนนั้น**
  กดได้ทั้งจากช่อง “เลือกชื่อ” คำอธิบายสีด้านขวา และชื่อในการ์ดจัดการเจ้าหน้าที่
  กล้องใช้เวลา 1.8 วินาที เคลื่อนแบบนุ่มไม่กระตุก
- พื้นที่ของคนที่เลือกจะ **ยกตัวสูงขึ้นและมีเส้นขอบเน้น** ส่วนพื้นที่ของคนอื่น
  หรี่ลงกลมกลืนกับพื้นหลัง แต่ยังมองเห็นและคลิกมอบหมายข้ามคนได้อยู่
  (การหรี่ทำที่ตัวสีไม่ใช่ความทึบ เพราะ `fill-extrusion-opacity` ของ MapLibre
  ไม่รองรับนิพจน์รายพื้นที่ มุมมอง 3 มิติจึงจะไม่หรี่ตามถ้าใช้ความทึบ)
- ปรับความเร็วกล้องทั้งระบบให้ช้าลง เห็นชัดขึ้นว่ากำลังไล่เข้าไปที่ไหน

## Version 5.0.0 — 27 สิงหาคม 2569

ปรับปรุงตามรายงานตรวจสอบระบบทั้ง 15 ข้อ พร้อมงานออกแบบใหม่ตามที่ผู้ดูแลร้องขอ

### งานตามที่ร้องขอ

- **ยอดเป็นจำนวนล้วน** ตัดคำว่า “บาท” และสัญลักษณ์ ฿ ออกจากทุกจุดที่ผู้ใช้เห็น
  ทั้งบนแผนที่ ป๊อปอัป การ์ดข้อมูล รายงาน Excel และ PDF
  ผู้ดูแลเปิด/ปิดการแสดงยอดบนหน้าจอแสดงผลได้ และผู้ดูปิดเฉพาะเครื่องตัวเองได้อีกชั้น
- **ธีมใหม่ สลับสว่าง/มืด** ปุ่มสลับ 3 สถานะ จำค่าไว้ในเครื่อง
  สีทั้งหมดรวมทั้งสีบนแผนที่ย้ายมาเป็น CSS token ชุดเดียวใน `tokens.css`
- **แผนที่เดียวกันทั้งสองหน้าจอ** รวมโค้ดแผนที่ที่เคยเขียนซ้ำสองชุดไว้ที่
  `map-engine.js` พร้อมลำดับเปิดตัวแบบค่อย ๆ ไล่เข้าและพื้นที่ยกตัวขึ้นจากพื้น

### แก้ตามผลตรวจ

| ข้อ | เรื่อง | สิ่งที่ทำ |
|---|---|---|
| ตร-01 | ArcGIS ล่มแล้วระบบตาย | ใช้ `data/court-tambon.geojson` ในเครื่องเป็นหลัก ArcGIS เป็นตัวเสริมแบบเบื้องหลัง ไม่มีทางทำให้หน้าเว็บพังอีก |
| ตร-02 | ดึงทั้งจังหวัดแล้วกรองในเบราว์เซอร์ | สร้างไฟล์ 85 ตำบลไว้ล่วงหน้า (46 KB) และเมื่อขอ ArcGIS ก็ถามเฉพาะ 6 อำเภอ ไม่ใช่ทั้งจังหวัด |
| ตร-03 | token เก็บถาวรใน localStorage | ย้ายไป `sessionStorage` หายเมื่อปิดเบราว์เซอร์ และลบรหัสที่รุ่นก่อนค้างไว้ให้อัตโนมัติ |
| ตร-04 | ยอดเปิดสาธารณะ | คงไว้ตามที่ผู้ดูแลตัดสินใจ เพิ่มสวิตช์ควบคุมและข้อความเตือนที่ตรงความจริงขึ้น |
| ตร-05 | พึ่ง CDN ไม่มี SRI | ย้าย MapLibre, SheetJS, html2canvas มาไว้ใน `vendor/` ไม่เรียกสคริปต์ภายนอกอีกเลย |
| ตร-06 | ข้อมูลภูมิศาสตร์สองชุดไม่ตรงกัน | รวมเป็นรูปแบบเดียวตอนสร้างไฟล์ และย้ายรหัสอำเภอไปไว้ใน `core.js` ที่เดียว |
| ตร-07 | บันทึกสองไฟล์แยก commit | เปลี่ยนเป็น Git Trees API หนึ่ง commit สำเร็จหรือล้มเหลวพร้อมกัน |
| ตร-08 | ดึงไฟล์เต็มทุก 30 วินาที | ใช้ ETag/If-None-Match และยืดรอบเป็น 90 วินาที ไม่มีการเปลี่ยนแปลงจะได้ 304 ตัวเปล่า |
| ตร-09 | โหลดไลบรารีส่งออกทุกครั้ง | โหลดเมื่อกดปุ่มส่งออกเท่านั้น |
| ตร-10 | ไม่มีร่องรอยว่าใครแก้อะไร | ข้อความ commit สรุปการเปลี่ยนแปลงพร้อมชื่อผู้บันทึกและเวลา |
| ตร-11 | ไม่มีการยืนยันตัวตน | ยังคงเป็นระบบผู้ดูแลคนเดียวตามที่ตัดสินใจ แต่บันทึกชื่อผู้บันทึกลงข้อมูลกลางแล้ว เตรียมทางไว้สำหรับระยะที่ 3 |
| ตร-12 | ย้อนรุ่นไม่ได้ | เพิ่มขั้นตอนติด tag ไว้ใน README และแยกไฟล์เป็นสัดส่วนให้ diff อ่านง่าย |
| ตร-13 | README อ้างชุดทดสอบที่ไม่มีอยู่ | เขียนชุดทดสอบจริง 26 รายการใน `tests/` พร้อม GitHub Actions |
| ตร-14 | เลขรุ่น CSS ไม่ตรงกันสองหน้า | ใช้ `APP_VERSION` ชุดเดียว และมีชุดทดสอบคอยจับถ้าเผลอใส่ไม่ตรง |
| ตร-15 | ไฟล์ตัวอย่างชื่อเพี้ยนอยู่ราก Repository | ย้ายไป `samples/price-import-example.csv` |

### แก้ระหว่างพัฒนา

- ป้ายบนแผนที่เคยกองอยู่มุมซ้ายบนทั้งหมด เพราะ CSS animation ที่เคลื่อน `transform`
  ไปทับ transform ที่ MapLibre ใช้จัดตำแหน่ง marker — เปลี่ยนเป็น animate เฉพาะ `opacity`
- การกันป้ายทับกันเดิมเดาความกว้างจากจำนวนตัวอักษร ซึ่งใช้กับภาษาไทยไม่ได้
  เปลี่ยนเป็นวัดขนาดจริงก่อนวาง และกันไม่ให้ป้ายไปทับปุ่มควบคุมหรือล้นขอบแผนที่
- ภาพรวมแผนที่เดิมเล็งกรอบทั้งจังหวัด ทำให้เขตศาลซึ่งอยู่ครึ่งตะวันตกดูเล็กและเบี้ยว
  เปลี่ยนมาเล็งกรอบ 6 อำเภอในเขตศาล โดยเผื่อระยะให้ยังเห็นอำเภอข้างเคียงเป็นฉากหลัง
